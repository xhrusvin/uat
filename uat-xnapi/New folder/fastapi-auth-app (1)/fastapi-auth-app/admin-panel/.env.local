# Port the Vite dev server listens on
VITE_ADMIN_PORT=8051

# API base URL — empty means Vite proxy handles it (pointing to API_PORT below)
VITE_API_URL=

# Port of the FastAPI backend (used by Vite proxy)
VITE_API_PORT=8050

# API key for /users/ endpoints
VITE_API_KEY=xh-uat-9f4a2c8b1d6e3f7a0b5c9d2e4f8a1b3c
